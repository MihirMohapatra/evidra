apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit
  namespace: evidra
spec:
  replicas: 2
  selector:
    matchLabels:
      app: audit
  template:
    metadata:
      labels:
        app: audit
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: audit
        image: ghcr.io/OWNER/evidra/audit:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8085
          name: http
        envFrom:
        - configMapRef:
            name: evidra-config
        env:
        - name: EVIDRA_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: evidra-secret
              key: database-url
        - name: EVIDRA_JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: evidra-secret
              key: jwt-secret
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8085
          initialDelaySeconds: 10
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 8085
          initialDelaySeconds: 5
          periodSeconds: 10
